name: build

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          submodule: recursive 

      - name: Install Zig
        run: sudo ./zig-install.sh
        
      - name: Compile
        run: zig build
